import { Box } from "@chakra-ui/react";

import TodoList from "../components/TodoList";

const Dashboard = () => {
  return (
    <Box>
      <TodoList />
    </Box>
  );
};

export default Dashboard;
